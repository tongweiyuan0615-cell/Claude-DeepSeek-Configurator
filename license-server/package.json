{
  "name": "claude-deepseek-license-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:apply:local": "wrangler d1 execute claude_deepseek_license --local --file=./schema.sql",
    "d1:apply:remote": "wrangler d1 execute claude_deepseek_license --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
